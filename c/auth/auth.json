/**
 * GEO IP AUTH
 * -----------
 * -----------
 * @blacklist {array} Takes IP address as string.
 * @whitelist {array} Takes IP Address as string.
 */
{
    "blacklist": ["173.164.*.*"],
    "whitelist": ["*.*.*.*"]
}